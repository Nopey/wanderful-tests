mapsize 10 ;
start
   create Bob 1 1 south ;
   if ( 1 < 3 ) then
      % the SELECT grammar expects a STATEMENTS here
      % STATEMENTS requries at least one statement.
      % And so, this is not valid.
   end
finish
